%%
%% Switching algebra in LaTeX
%%
\documentclass[a4paper]{article}

\makeatletter
\def\band(#1,#2){\@band{#1}{#2}}
\newcommand{\@band}[2]{\ifnum#1=0 0\else\ifnum#2=0 0\else 1\fi\fi}
\makeatother

\begin{document}

\band(0,0) % ok
\band(0,1) % ok
\band(1,0) % ok
\band(1,1) % ok

\band(0,\band(0,0)) % extra )
\band(0,\band(0,1)) % extra )
\band(0,\band(1,0)) % extra )
\band(0,\band(1,1)) % extra )
\band(1,\band(0,0)) % ok
\band(1,\band(0,1)) % ok
\band(1,\band(1,0)) % ok
\band(1,\band(1,1)) % ok

\band(\band(0,0),0) % ok
\band(\band(0,1),0) % ok
\band(\band(1,0),0) % ok
\band(\band(1,1),0) % ok
\band(\band(0,0),1) % ok
\band(\band(0,1),1) % ok
\band(\band(1,0),1) % ok
\band(\band(1,1),1) % ok

\band(\band(1,1),\band(1,1)) % ok
\band(\band(\band(0,1),\band(1,1)),\band(1,1)) % error Missing = inserted


\end{document}